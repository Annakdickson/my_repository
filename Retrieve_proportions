def plusMinus(arr):
    pos = 0
    neg = 0 
    zero = 0
    total = len(arr)
    for i in arr:
        if i > 0:
            pos += 1
        elif i < 0:
            neg += 1
        elif i == 0:
            zero += 1
    print(pos/total)
    print(neg/total)
    print(zero/total)
